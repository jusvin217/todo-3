export const CLIENT_ID = '<YOUR_GOOGLE_CLIENT_ID_HERE>';
export const SCOPES = ['https://www.googleapis.com/auth/calendar.events'];

// This file contains helper placeholders. For a full flow, implement PKCE + loopback server
// or open system browser and capture auth code. The electron main process exposes
// window.electronAPI.openExternal(url) and save/get token helpers.
